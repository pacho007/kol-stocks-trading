[profile.default]
src = "src"
test = "test"
script = "script"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
robinhood_testnet = "https://rpc.testnet.chain.robinhood.com"
robinhood_mainnet = "https://rpc.mainnet.chain.robinhood.com"

[etherscan]
# Fill in once Robinhood Chain's verification endpoint/API key story is
# confirmed (see docs.robinhood.com/chain) — `forge verify-contract` needs
# this to verify SharpsMarket post-deploy.
